#!/bin/sh
set -eu

# Compatibility alias for installations and automation created before the
# desktop companion was consistently branded Codex Router.
repo_dir=$(CDPATH='' cd -- "$(dirname -- "$0")/.." && pwd)
exec "$repo_dir/bin/codex-router-tray" "$@"
